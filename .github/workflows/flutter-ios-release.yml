name: Flutter iOS Release

permissions:
  contents: write

on:
  workflow_dispatch:
    inputs:
      tag:
        description: Existing GitHub Release tag
        required: true
        default: v0.1.8-suicang-stable
      version:
        description: Display version
        required: true
        default: 0.1.8

jobs:
  build-unsigned-ipa:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.tag }}
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true
      - name: Install dependencies
        run: flutter pub get
      - name: Generate Drift sources
        run: dart run build_runner build --delete-conflicting-outputs
      - name: Generate iOS host when absent
        run: test -d ios || flutter create --platforms=ios --project-name=suicang_eh .
      - name: Configure official iOS bundle identity
        run: |
          /usr/libexec/PlistBuddy -c "Set :CFBundleIdentifier com.suicang.eh" ios/Runner/Info.plist || true
          /usr/libexec/PlistBuddy -c "Set :CFBundleDisplayName Suicang EH" ios/Runner/Info.plist || true
          /usr/libexec/PlistBuddy -c "Set :CFBundleName SuicangEH" ios/Runner/Info.plist || true
          sed -i.bak 's/PRODUCT_BUNDLE_IDENTIFIER = com.example.suicangEh/PRODUCT_BUNDLE_IDENTIFIER = com.suicang.eh/g' ios/Runner.xcodeproj/project.pbxproj || true
          perl -0pi -e 's/android: true/android: false/' pubspec.yaml
          dart run flutter_launcher_icons
      - name: Install CocoaPods dependencies
        run: cd ios && pod install --repo-update
      - name: Build unsigned iOS archive
        run: |
          flutter build ipa --release --no-codesign \
            --build-name=${{ inputs.version }} \
            --build-number=1 \
            --dart-define=SUICANG_IOS_BUNDLE_ID=com.suicang.eh

      - name: Package unsigned IPA container
        run: |
          set -e
          mkdir -p release/Payload
          cp -R build/ios/archive/Runner.xcarchive/Products/Applications/Runner.app release/Payload/TaroEH.app
          find release/Payload/TaroEH.app -type f | while read file; do
            file "$file" | grep -q 'Mach-O' && chmod 755 "$file" || true
          done
          chmod 755 release/Payload/TaroEH.app/TaroEH || chmod 755 release/Payload/TaroEH.app/Runner
          ditto -c -k --sequesterRsrc --keepParent release/Payload release/suicang-eh-${{ inputs.version }}-unsigned.ipa
          unzip -t release/suicang-eh-${{ inputs.version }}-unsigned.ipa
          shasum -a 256 release/suicang-eh-${{ inputs.version }}-unsigned.ipa | tee release/SHA256SUMS.txt
      - uses: actions/upload-artifact@v4
        with:
          name: suicang-eh-ios-${{ inputs.version }}-unsigned
          path: |
            release/suicang-eh-${{ inputs.version }}-unsigned.ipa
            release/SHA256SUMS.txt
      - name: Upload IPA to existing release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ inputs.tag }}
          files: |
            release/suicang-eh-${{ inputs.version }}-unsigned.ipa
            release/SHA256SUMS.txt
          overwrite_files: true
